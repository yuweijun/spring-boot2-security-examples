# spring-boot2-security-examples

# compile and build

    $ gradle compileJava
    $ gradle build

# docker compose form mysql and mongo

    $ cd docker
    $ docker-compose up -d
    $ docker-compose stop
